"use client";

import ReactDOM from "react-dom";

export function PreloadResources() {

    // ReactDOM.preconnect("",{crossOrigin:"anonymous"})
    // ReactDOM.preconnect("https://fonts.gstatic.com", {
    //   crossOrigin: "anonymous",
    // });

    // ReactDOM.preload("/logo.svg", { as: "image" });

    // ReactDOM.prefetchDNS("https://www.google-analytics.com");
    // ReactDOM.prefetchDNS("https://www.googletagmanager.com");
    
    return "..."
}